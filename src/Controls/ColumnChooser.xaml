<UserControl
    x:Class="daVinci.Controls.ColumnChooser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:LUIConverter="clr-namespace:leonardo.Converter;assembly=leonardo-wpf"
    xmlns:LuiAttached="clr-namespace:leonardo.AttachedProperties;assembly=leonardo-wpf"
    xmlns:LuiControls="clr-namespace:leonardo.Controls;assembly=leonardo-wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/leonardo-wpf;component/Styles/LUIStyles.xaml" />
                <ResourceDictionary Source="pack://application:,,,/leonardo-wpf;component/Styles/LUI_ColumnOrderAccordionItemStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>


            <LUIConverter:TypeToStyleSelector
                x:Key="TypeToStyleSelector"
                DefaultStyle="{StaticResource LUI_ColumnOrderAccordionItemStyle}"
                ItemStyle1="{StaticResource LUI_ColumnOrderAccordionItemStyle}"
                ItemType1="{x:Type LuiControls:LuiAccordionItem}" />
        </ResourceDictionary>

    </UserControl.Resources>
    <StackPanel>
        <Border
            Name="MyBorder"
            Height="28"
            Margin="10,5,10,5"
            BorderBrush="Black">
            <LuiControls:LuiToggleButton
                x:Name="togglebutton"
                LuiAttached:ThemeProperties.IconRight="lui_icon_triangle_bottom"
                Content="Spalte Hinzufügen" />
        </Border>
        <LuiControls:LuiDialog
            x:Name="dialog"
            Content="Hallo Dialog!"
            IsOpen="{Binding IsChecked, ElementName=togglebutton}"
            PanelHeight="200"
            PanelWidth="200"
            Placement="Bottom"
            PlacementTarget="{Binding ElementName=togglebutton}" />
        <TextBlock
            Margin="10,5,0,0"
            FontFamily="{StaticResource QlikViewSans}"
            FontSize="12"
            FontWeight="Bold"
            Foreground="{StaticResource LUI.Brush.Grayscale30}"
            Text="Spalten" />
        <LuiControls:LuiSeparator Height="3" />
        <LuiControls:LuiAccordion
            ItemContainerStyleSelector="{StaticResource TypeToStyleSelector}"
            ItemsSource="{Binding Columns}">

            <!--<LuiControls:LuiAccordionItem
                Content="Inhalt"
                Header="Item" />-->
        </LuiControls:LuiAccordion>
    </StackPanel>
</UserControl>
